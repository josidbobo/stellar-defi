import * as Stellar from '@stellar/wallet-sdk';
import {account, KeyPair, TransactionBuilder, Asset, Operation, LiquidityPoolAsset, getLiquidityPoolId, BASE_FEE, Networks} from 'stellar-sdk';

async function createLiquidity()  {
    return 
}